# GitHub reads this file and shows a Sponsor button at the top of the repository page.
#
# github: points at the owner's own account. You must enrol at github.com/sponsors
# first, or the button never appears. Note also that GitHub Sponsors cannot currently
# receive payments in mainland China (it requires Stripe Connect).
#
# Fill in whichever of the following you have actually set up, and delete the rest.
# Never put someone else's handle here. custom takes any URL.

github: CAMS-NIR
# ko_fi:
# buy_me_a_coffee:
# custom: ["https://afdian.com/a/YOUR-NAME"]
